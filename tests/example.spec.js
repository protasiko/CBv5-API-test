import {expect} from 'chai'

describe('First test', () => {
    it('Check if sum is correct', () => {
        expect(5+2).to.eq(7)
    })

    it('Check multiplication is correct', () => {
        expect(4*3).to.eq(12)
    })
})
